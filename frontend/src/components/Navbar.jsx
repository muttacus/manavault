function Navbar() {
  return (
    <header className="navbar">
      <h1>🧙 ManaVault</h1>

      <div>
        <button>Search Cards</button>
        <button>Store Login</button>
      </div>
    </header>
  );
}

export default Navbar;